Repository secret scan summary
=============================

Findings (grep-based):
- frontend/src/firebase.ts: contains Firebase web config (apiKey, projectId). Web API key is expected public but ensure App Check and rules are configured.
- backend/src/main/resources/* previous service-account JSON contained a private_key (rotated and removed). Rotate keys immediately.
- backend/service code references `JWT_SECRET` environment variable (ensure set and strong).
- mvnw scripts reference MVNW_USERNAME/MVNW_PASSWORD placeholders (check CI configs).

Actionable items:
- Rotate Firebase service account key now.
- Purge the secret JSON from git history (BFG or git filter-repo) and force-push.
- Set `FIREBASE_SERVICE_ACCOUNT_B64` or `FIREBASE_SERVICE_ACCOUNT` in Railway secrets and `JWT_SECRET` to a strong value.
- Run the `tools/secret-scan.sh` script locally and review matches.
