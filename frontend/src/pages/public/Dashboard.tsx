import { useState } from "react";
import { dashboard, showToast } from "../../api/auth";

export default function Dashboard() {
  return (
    <div style={{ padding: 20 }}>
      <h1>Dashboard</h1>
    </div>
  );
}